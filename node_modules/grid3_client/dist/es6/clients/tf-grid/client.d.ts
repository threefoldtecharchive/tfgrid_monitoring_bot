import AwaitLock from "await-lock";
import { KeypairType } from "../../zos/deployment";
import { Balance } from "./balance";
import { Contracts } from "./contracts";
import { KVStore } from "./kvstore";
import { Twins } from "./twins";
declare class TFClient {
    url: string;
    mnemonic: string;
    storeSecret: string | Uint8Array;
    keypairType: KeypairType;
    static clients: Record<string, TFClient>;
    static lock: AwaitLock;
    client: any;
    contracts: Contracts;
    twins: Twins;
    kvStore: KVStore;
    balance: Balance;
    constructor(url: string, mnemonic: string, storeSecret: string | Uint8Array, keypairType?: KeypairType);
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    isConnected(): boolean;
    _polkaMethodsWrapper(func: (args: unknown[]) => unknown, args: unknown[]): Promise<any>;
    queryChain(func: (args: unknown[]) => unknown, args: unknown[]): Promise<any>;
    rpcCall(func: (args: unknown[]) => unknown, args: unknown[]): Promise<any>;
    private _applyExtrinsic;
    applyExtrinsic(func: (args: unknown[]) => unknown, args: unknown[], resultSection: string, resultNames: string[]): Promise<any>;
}
export { TFClient };
//# sourceMappingURL=client.d.ts.map
import { MessageBusClientInterface } from "ts-rmb-client-base";
import { GridClientConfig, NetworkEnv } from "./config";
import * as modules from "./modules/index";
import { BackendStorageType } from "./storage/backend";
import BackendStorageInterface from "./storage/BackendStorageInterface";
import { KeypairType } from "./zos/deployment";
declare class GridClient {
    network: NetworkEnv;
    mnemonic: string;
    storeSecret: string | Uint8Array;
    rmbClient: MessageBusClientInterface;
    projectName: string;
    backendStorageType: BackendStorageType;
    keypairType: KeypairType;
    backendStorage: BackendStorageInterface;
    static config: GridClientConfig;
    machines: modules.machines;
    k8s: modules.k8s;
    zdbs: modules.zdbs;
    gateway: modules.gateway;
    qsfs_zdbs: modules.qsfs_zdbs;
    zos: modules.zos;
    contracts: modules.contracts;
    twins: modules.twins;
    kvstore: modules.kvstore;
    balance: modules.balance;
    capacity: modules.capacity;
    twinId: number;
    nodes: modules.nodes;
    algorand: modules.algorand;
    tfchain: modules.tfchain;
    stellar: modules.stellar;
    modules: string[];
    constructor(network: NetworkEnv, mnemonic: string, storeSecret: string | Uint8Array, rmbClient: MessageBusClientInterface, projectName?: string, backendStorageType?: BackendStorageType, keypairType?: KeypairType, backendStorage?: BackendStorageInterface);
    connect(): Promise<void>;
    _connect(): void;
    getDefaultUrls(network: NetworkEnv): Record<string, string>;
    disconnect(): Promise<void>;
    disconnectAndExit(): Promise<void>;
    invoke(message: any, args: any): Promise<any>;
}
export { GridClient };
//# sourceMappingURL=client.d.ts.map